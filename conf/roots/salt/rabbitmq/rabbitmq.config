[
  {rabbit, [
{%- if pillar.get('rabbitmq_cluster_nodes', []) %}
    {cluster_nodes, {[{% for node in pillar['rabbitmq_cluster_nodes'] %}'rabbit@{{ node }}'{% if not loop.last %}, {% endif %}{% endfor %}], {{ pillar.get('rabbitmq_cluster_node_type', 'disc') }}}}
{%- endif %}
  ]}
].
