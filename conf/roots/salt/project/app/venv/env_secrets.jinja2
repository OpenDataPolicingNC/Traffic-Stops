{% for key, val in pillar['secrets'].iteritems() -%}
export {{ key }}={{ val }}
{% endfor -%}
{% for key, val in pillar.get('environment_variables',{}).iteritems() -%}
export {{ key }}={{ val }}
{% endfor -%}
export DJANGO_SETTINGS_MODULE="{{ settings }}"
